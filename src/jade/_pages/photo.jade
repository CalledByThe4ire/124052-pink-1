extends ../_layouts/base

block variables

block content
  +b.intro
    +e.title-wrap
      +e.H1.title Фотографии пользователей
    .box
      +e.text-wrap
        p.text Взгляните на&nbsp;фотографии, которые выкладывают наши пользователи! Видите, как не&nbsp;хватает ваших?

  +b.SECTION.feed
    each item in site.data.feed
      if item.top
        +post(item)
    
    .container
      +e.grid
        each item in site.data.feed
          if !item.top
            +post(item)

  .container
    p.text Затмите их&nbsp;всех! Выкладывайте все, что накопилось в&nbsp;телефоне!

  +editor
